import NextAuth from "next-auth";

// declaring types for authentication sessions and users
declare module "next-auth" {
  interface Session {
    user: {
      id: string;
      name?: string | null;
      email?: string | null;
      image?: string | null;
    };
  }
}
